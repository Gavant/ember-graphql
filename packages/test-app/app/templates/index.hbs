{{page-title "Application"}}
{{outlet}}
<ul>
    {{#each this.model.data.users.data as |user|}}
        <li>#{{user.id}} {{user.name}} ({{user.email}})</li>
    {{/each}}
</ul>
{{log this.model.error}}
{{if this.model.loading "Loading..."}}